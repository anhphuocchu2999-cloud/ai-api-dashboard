# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-16

## 当前阶段

`Stage 8B-V：MiMo／爱黄牛真实数据链路收口`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- 起始提交：`eec94f5d4053632140b1700786098bf3f840c94a`
- MiMo双域名路由：`6572c53cbfa2d419eb8dda7b4b6cc211da7113c4`
- MiMo持久化服务类型：`eb97daf69532b5f9b1f94520d25f276df0e96aa8`
- MiMo完整账户用量：`294f8f78c9aae174dc6d3698e281063d681346ec`
- 爱黄牛API／网页登录解耦：`cd09d43e5d2b3408e1436c34cb5093eeaf0df3c6`
- 设置页安全预览：`733a6206ae61aea1274e27a12b0d776143ae36b3`
- 当前状态：云端代码已推送；尚未声称最新版本编译、安装或真机验收成功

## 本轮修复目标

不再逐个补字段，而是收口以下真实数据链路：

1. 服务路由；
2. API Key与网页登录凭据边界；
3. 平台字段解析；
4. 设置页当前真实数据；
5. 近期消耗累计输入；
6. 缓存与实时状态区分。

## MiMo修复

- `api.xiaomimimo.com`与`platform.xiaomimimo.com`都识别为MiMo；
- 删除“没有Cookie时把模型API Key当Cookie”的历史回退；
- 没有网页登录Cookie时明确要求登录，不向账户接口发送模型API Key；
- 余额接口严格区分字段缺失和平台真实返回0；
- 展示余额、赠送余额、现金余额、冻结余额、可用透支、透支额度；
- 用量接口补齐：
  - 本月消耗；
  - 累计消耗；
  - 累计请求；
  - 累计Token；
  - 输入Token；
  - 输出Token；
  - 缓存Token；
  - Web搜索；
  - RPM、TPM、Query TPM、并发；
- 近期消耗按MiMo平台账户身份积累，不再绑定某个模型名称；
- 余额成功但用量失败时明确显示部分成功原因。

## 爱黄牛修复

- 模型API Key只请求`/v1/usage`；
- 网页Bearer Token只请求`/api/v1/user/profile`；
- 未登录平台账户时，累计请求、累计Token、累计实际消耗仍可读取；
- API用量成功即可开始近期1／6／12／24小时计算，不再要求网页登录；
- API Base统一移除末尾`/v1`，避免`/v1/v1/usage`和`/v1/api/v1/user/profile`；
- 余额、充值与实际消费统一使用BigDecimal解析；
- 模型名采用不区分大小写的精确匹配；未匹配时明确提示当前模型没有独立用量；
- 同Host两类请求保持至少500毫秒间隔；
- API-only时可显示累计用量，登录后再合并余额、累计充值、并发、账户状态和最近活跃。

## 设置页修复

- 同一API地址匹配多个槽位时不再取第一个，直接停止展示并提示，避免串号；
- 网页授权优先读取平台公共授权键，再兼容当前槽位键；
- `isFallback=true`时明确显示“本地缓存”，不冒充当前云端读取；
- MiMo与爱黄牛新增真实字段会自动进入“当前真实数据”；
- 爱黄牛近期消耗能力只依赖API连接；
- 数据来源继续区分`云端 API`、`云端账户`、`本地测算`、`本地缓存`。

## 未扩大范围

- 未修改DeepSeek本轮已通过的账户汇总字段映射；
- 未修改Kimi／NewAPI业务逻辑；
- 未修改Widget布局；
- 未卸载、未清除数据；
- 未声称编译成功。

## 本地验证要求

1. 拉取指定HEAD后工作区必须干净；
2. 只编译一次；
3. 编译成功后只覆盖安装一次；
4. MiMo：设置页应显示余额构成、本月／累计消耗、累计请求、各类Token和限流字段；
5. MiMo：断开网页登录后不得把API Key当Cookie，页面应明确要求登录；
6. 爱黄牛：只连接API、不登录账户时仍应显示累计请求、Token、消耗和近期统计状态；
7. 爱黄牛：登录后再补充余额、累计充值、并发、状态和最近活跃；
8. API地址以`/v1`结尾时爱黄牛不能出现重复路径；
9. 网络失败回退时设置页必须标记本地缓存；
10. DeepSeek、Kimi、模型检测、网页登录和桌面刷新不得回归。

## 本地执行边界

OperitAI只允许：拉取、检查HEAD和工作区、编译一次、覆盖安装一次、启动和汇报。

禁止：修改代码、自动修复、重复编译、提交、推送、卸载、`pm clear`或清除应用数据。
