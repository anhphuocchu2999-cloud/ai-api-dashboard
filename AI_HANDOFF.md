# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-16

## 当前阶段

`Stage 8B-X：模型实例独立最近成功缓存`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- 前置已验收版本：`38c38bf7603b1d96784313f80f77e4c8b5ebc8c9`
- 请求实例身份：`91849aef667457656d8656bdf26120c860185e5a`
- 实例缓存隔离：`33ad0b3a9fbb79ddd8e524df2c8cf08b930fc89e`
- 网络层缓存身份：`af9bc7774e647e6728480932c28345f80ebe5f51`
- 当前状态：云端代码和独立Kotlin静态检查已完成；尚未声称Android完整编译、安装或真机验收成功

## 本轮只修复一个Bug

旧实现按Adapter平台名保存最近成功数据。两个槽位使用同一平台但配置不同，网络失败时可能读取同一份缓存。

## 当前修复

- `AdapterRequest`新增`instanceId`，仅用于本地缓存隔离，不发送给平台；
- 网络层根据实例、API地址、模型和认证配置生成SHA-256本地指纹；
- 原始认证内容不会出现在缓存键、日志或界面；
- 成功数据只有绑定当前缓存身份后才写入；
- 临时网络错误只读取当前身份自己的缓存；
- 配置或平台账户变化后自动使用新的缓存空间；
- 旧固定槽位键继续兼容读取；
- 缓存数据继续明确标记为Fallback，不冒充实时数据。

## 静态检查

已使用独立Kotlin编译桩检查：

- `AdapterRequest.kt`
- `WidgetData.kt`
- `NetworkAwareAdapter.kt`

结果通过，仅有测试桩未使用参数警告。该检查不等同于Android完整`assembleDebug`。

## 本地验证要求

1. 拉取指定HEAD后工作区必须干净；
2. 只执行一次`assembleDebug`；
3. 编译成功后只覆盖安装一次；
4. 两个槽位配置同一平台但使用不同账户或模型，分别成功刷新；
5. 断网后再次刷新，两张卡片必须各自保留自己的数据；
6. 切换平台账户后不得显示上一个账户缓存；
7. 输入、模型检测、网页登录、真实数据预览和单槽位Fallback不得回归。

## 下一项Bug

余额来源成功、用量来源失败时，残缺结果可能覆盖以前更完整的缓存。按阶段规则，本轮通过Android编译和真机验收后再处理。

## 本地执行边界

OperitAI只允许拉取、检查、编译一次、覆盖安装一次、启动和汇报。

禁止修改代码、自动修复、重复编译、提交、推送、卸载或清除应用数据。
