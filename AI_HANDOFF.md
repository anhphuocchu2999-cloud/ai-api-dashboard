# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-15

## 当前阶段

`Stage 8B-U：设置页展示当前真实数据与可用能力`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- 桌面真实动态数据基础：`fcd7bce1a816f1eec3ab359dfceffdceb20c8e20`
- 设置页能力说明：`78ae5f6147733a8245fe9f7e8ee0c1437069be5b`
- Compose错误导入修复：`9ae71cdb76ce32be9ebc562525d94eb92b6321fe`
- 设置页当前真实数据预览：`8dcd96273fc2e4c164c31e9b7f91f2bf746ed407`
- 产品规则补充：`cf8283a3973c73f41cb9cb538aecc4651783e4e0`
- 当前状态：云端代码已完成；最新版本尚未完成本地编译、安装和真机验收

## 用户纠正后的目标

设置页不能只列“理论上能展示什么”，还必须直接显示当前账户本次真正返回的数值。

卡片现在分为两部分：

1. `当前真实数据`
2. `这个槽位还能展示什么`

## 当前真实数据

当API地址、API Key和模型已经完整连接时：

- 进入设置页自动请求一次；
- 使用与桌面Widget相同的Adapter；
- 使用当前API Key及已保存的平台账户授权；
- 显示本次真正返回的余额、消耗、Token、请求、额度和辅助数据；
- 显示`云端 API`、`云端账户`或`本地测算`来源；
- 显示本次读取时间；
- 提供“刷新真实数据”按钮；
- 接口未返回时明确显示未返回、暂无可计算数据或失败原因；
- 不填充演示数字，不把能力说明冒充当前数据。

## 平台数据

### Kimi / NewAPI

- 云端API：剩余次数、总额、已用次数、调用次数、Billing额度和用量；
- 本地测算：近1／6／12／24小时调用变化。

### MiMo

- 云端账户：余额构成、累计金额、累计请求、累计Token、缓存Token、Web搜索、RPM、TPM、并发；
- 本地测算：近1／6／12／24小时请求、Token和金额。

### DeepSeek

- 云端API：总余额、充值余额、赠送余额；
- 云端账户：本月消耗、累计消耗、本月Token、预计可用Token；
- 本地测算：近1／6／12／24小时Token和金额；
- 没有确认可靠调用次数字段时不显示调用次数。

### 爱黄牛

- 云端API：累计请求、累计Token、累计实际消耗；
- 云端账户：余额、累计充值、并发、账户状态、最近活跃；
- 本地测算：近1／6／12／24小时调用、Token和金额。

## 修改文件

- `SlotDataCapability.kt`
  - 新增设置页真实请求和数据预览；
  - 保留能力说明；
  - 显示读取时间和手动刷新入口；
  - 同一API地址存在多个完整槽位时明确提示当前选择规则。
- `PROJECT_STAGE_8B_U.md`
  - 将“能力说明”修正为“当前真实值＋能力说明”。

此前桌面真实动态数据和设置页布局继续保留。

## 真机验收

1. 进入已连接槽位时出现“当前真实数据”；
2. DeepSeek应至少返回官方API余额及余额组成；
3. 平台账户有效时应继续返回账户汇总数据；
4. 每项真实值旁边标明`云端 API`、`云端账户`或`本地测算`；
5. 本地历史不足时明确显示正在积累或暂无可计算数据；
6. 点击“刷新真实数据”会重新请求；
7. 请求失败时显示真实原因，不展示演示数字；
8. 下方继续展示“这个槽位还能展示什么”；
9. MiMo、爱黄牛、Kimi分别显示对应真实返回值；
10. 模型检测、网页登录和桌面Widget刷新不得回归。

## 本地执行边界

仅拉取、检查HEAD、编译一次、覆盖安装一次、启动和汇报。不要修改代码、重复编译、卸载或清除应用数据。
