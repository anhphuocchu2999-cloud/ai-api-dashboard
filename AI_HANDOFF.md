# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-15

## 当前阶段

`Stage 8B-T：桌面 Widget 真实动态数据与来源标识`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- 三平台近期消耗基础：`92e9fd68e6ed42c7cb6c2716a355b53a6836bbd2`
- 数据来源模型：`577d219b3750e9a4ee0ebb8bd829c4a843f3caf4`
- Widget 最终渲染收口：`3d5b28e2bda3588301d0fdd0852fd91310d39839`
- 当前状态：云端代码已完成；尚未声称本地编译、安装或真机验收成功

## 用户确认目标

四张桌面卡片实际显示的内容必须全部真实、动态、可追溯：

1. 平台接口直接返回的数字标记为 `云端·`；
2. 手机使用云端真实累计值计算的数字标记为 `本地·`；
3. 断网继续展示上次成功值时标记为 `缓存·`；
4. 不展示演示数字、写死数字、虚假 0 值或无依据估算；
5. 历史不足时只展示统计状态，不冒充近 1／6／12／24 小时数据。

## 卡片结构

- 主指标：云端真实返回；
- 动态位①：近期消耗；
- 百分比：本地基于云端真实值计算；
- 动态位②：云端真实返回的其他重要指标。

## 本轮最终实现

### `WidgetData.kt`

- `DisplayMetric` 统一识别数据来源并写入桌面标签；
- 普通余额、累计值、额度、限流等显示 `云端·`；
- 近 1／6／12／24 小时、过去若干分钟、余额变化等显示 `本地·`；
- 百分比明确显示 `本地·`；
- 成功获取核心数据但近期历史不足时显示 `近期消耗 暂无可计算数据`；
- 缓存兼容旧数据，已有来源前缀不会重复添加；
- 新五层真实指标参与空数据判断。

### `BalanceWidgetProvider.kt`

- 直接渲染数据模型已经确认的来源，不再二次添加，避免 `云端·云端` 或 `本地·本地`；
- 缓存值统一改为 `缓存·`；
- 四张卡片均使用同一真实渲染规则；
- 单击 Widget 立即重新请求平台；
- 同一 Host 保持串行且间隔 1 秒；
- 请求真正完成后才显示更新时间；
- 配置缺失或请求失败不展示虚假数字。

### 自动刷新

- Android Widget 正式更新周期：30 分钟；
- 每次自动或手动刷新都会重新请求平台数据；
- `RecentUsageTracker` 保留最近 30 小时的真实累计快照；
- 支持近 1 小时、6 小时、12 小时、24 小时；
- 累计值下降时自动重建基准；
- 不保存 API Key、Cookie、Token 或接口原文。

## 四类卡片真实数据

### Kimi / NewAPI

- 云端：剩余次数、总额、已用次数、调用次数、Billing 额度与用量；
- 本地：根据云端累计调用计算近期调用变化。

### MiMo

- 云端：余额及余额构成、累计请求、累计 Token、累计金额、缓存 Token、Web 搜索、RPM、TPM、并发；
- 本地：根据累计请求、Token、金额计算近期消耗。

### DeepSeek

- 云端：API 余额、充值余额、赠送余额、网页账户累计金额、Token、预计可用 Token；
- 本地：根据真实累计值计算近期 Token 和金额；没有确认的请求次数字段时不显示次数。

### 爱黄牛

- 云端：余额、累计充值、累计请求、累计 Token、累计实际消耗、并发和账户状态；
- 本地：根据真实累计请求、Token、金额计算近期消耗。

## 真机验收

1. 四张卡片主指标应显示 `云端·`；
2. 近期消耗应显示 `本地·`；
3. 百分比应显示 `本地·`；
4. 动态位②应显示 `云端·`；
5. 不得出现重复来源标签；
6. 断网时应显示 `缓存·`；
7. 单击 Widget 应立即刷新；
8. 首次统计不得出现虚假数字；
9. 产生真实调用并积累快照后，近期数字应随刷新变化；
10. 余额、网页登录和断网兜底不得回归。

## 本地执行边界

只允许拉取、检查 HEAD、编译一次、覆盖安装一次、启动和汇报。

禁止修改代码、自动修复、重复编译、提交、推送、卸载或清除应用数据。
