# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-16

## 当前阶段

`Stage 8B-W：槽位输入稳定与 MiMo 平台账户数据预览`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- 前置真实数据链路：`061a55bb50aaf7ec7404e9dd0a1f353d64d395cc`
- 输入状态修复：`17b31a98199bf9239659801f97ca0f9c8109d751`
- 当前槽位／MiMo账户预览：`b1135d87c49ee3fdb4f09389df295d541915b56b`
- 当前状态：云端代码已推送；尚未声称最新版本编译、安装或真机验收成功

## 用户真机暴露的问题

1. API 地址和 API Key 输入时频繁卡顿，无法顺畅连续输入；
2. 地址和 Key 已清空，但页面仍残留“连接成功／已选择模型”；
3. MiMo 模型 API 可以检测模型，但不能返回金额；
4. 登录 MiMo 网站后，设置页仍没有显示平台账户余额和用量数据。

## 根因

### 输入卡顿

旧逻辑每输入一个字符就执行：

- 写固定槽位 JSON；
- 调用 `ConfigRepository.saveConfig()`；
- 同步 ModelInstance；
- 触发大量主线程 JSON／SharedPreferences 工作。

### 错误成功状态

`ConnectionStatus.Success` 是内存状态；修改或清空配置时没有同步清除，所以会出现空配置仍显示旧模型成功。

### MiMo 金额来源

MiMo 模型 API 只负责模型连接和模型列表，不承诺返回余额或消费金额。余额、金额、Token、请求和限流来自 `platform.xiaomimimo.com` 网页账户接口。

旧设置页只有完整 API 地址、Key、模型均存在时才读取数据，导致 MiMo 即使网页登录成功，也可能因模型 API 未完成而不读取账户数据。

## 当前修复

### 连续输入

- 输入期间只更新 Compose 内存草稿；
- 不再每个字符写磁盘或同步实例仓库；
- 在以下时机保存：
  - 点击返回；
  - 点击检测模型；
  - 选择模型；
  - 打开网页登录；
  - 切换槽位开关。
- 打开网页登录前先保存当前草稿，防止返回 `onResume` 时被旧配置覆盖。

### 状态一致性

- 地址或 Key 修改时立即清空旧连接状态；
- 修改地址／Key时同时清空旧模型；
- Activity 恢复时清除旧检测卡片；
- “连接成功”只在当前地址、Key和模型完整，且成功模型与当前模型一致时显示。

### 当前槽位真实数据

- 设置页不再按 API 地址搜索第一个配置；
- 直接使用当前槽位的 slotId、API 地址、API Key和模型请求；
- 避免同地址多槽位串号。

### MiMo 平台账户预览

- MiMo 地址已识别且网页登录有效时，即使模型 API 尚未完成，也可以读取账户数据；
- 页面明确说明模型 API 不返回金额；
- 账户数据继续通过平台公共授权键读取；
- 可显示余额构成、本月／累计消耗、请求、Token和限流字段；
- 如果网页登录凭据失效或接口失败，页面显示真实错误，不补假数据。

## 修改文件

- `MainActivity.kt`
- `SlotDataCapability.kt`
- `AI_HANDOFF.md`

## 本地验证要求

1. API 地址可以连续输入，不再逐字符明显卡顿；
2. API Key可以连续输入；
3. 清空或修改地址／Key后，旧“连接成功”立即消失；
4. 输入完成后点击检测，模型选择和保存正常；
5. 打开网页登录前当前输入不会丢失；
6. MiMo模型API不显示金额属于正常产品行为；
7. MiMo平台账户已连接时，上方“当前真实数据”应直接读取余额和用量，不强制要求模型API先返回金额；
8. 页面应显示真实成功数据或明确错误原因；
9. DeepSeek、爱黄牛、Kimi和Widget不得回归。

## 本地执行边界

OperitAI只允许：拉取、检查HEAD和工作区、编译一次、覆盖安装一次、启动和汇报。

禁止：修改代码、自动修复、重复编译、提交、推送、卸载、`pm clear`或清除应用数据。
