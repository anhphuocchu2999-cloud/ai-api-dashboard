# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-15

## 当前阶段

`Stage 8B-T：桌面 Widget 真实动态数据与来源标识`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- Widget 渲染与来源标识：`476678173d5bcf525dbaef294105e83f17c96472`
- 正式 Widget 自动刷新周期：`7e6c9a2b274fc505fead4a55916407bddd02878b`
- 数据模型真实性收口：`ed9e4fd9a07d62d4f0e8a8ef38e437b6aafadaac`
- 当前状态：云端代码已推送，下一步只进行一次本地编译、覆盖安装和真机验收

## 用户确认目标

四张桌面卡片实际显示的内容必须全部是真实、动态、可追溯的数据：

1. 平台接口直接返回的数据标记为“云端”；
2. 使用平台真实累计值在手机上计算的数据标记为“本地”；
3. 断网使用上次成功数据时标记为“云端缓存”或“缓存”；
4. 不使用演示数字、写死数字、虚假 0 值或无依据估算；
5. 历史不足时只显示“近期消耗统计中…”或“暂无可计算数据”。

## 卡片结构

- 主指标：云端真实返回；
- 动态位①：本地计算的近期消耗；
- 百分比：本地根据云端真实值计算；
- 动态位②：云端真实返回的其他重要数据。

## 本轮实现

### `BalanceWidgetProvider.kt`

- 四张卡片统一显示“云端 / 本地 / 云端缓存”来源；
- 点击 Widget 任意位置立即真实刷新，不再要求连续点击；
- Kimi 累计调用统一交给 `RecentUsageTracker` 计算；
- MiMo、DeepSeek、爱黄牛继续使用各自 Adapter 的真实累计数据；
- 同一 Host 串行请求，间隔 1 秒；
- 配置缺失或请求失败不展示虚假数字；
- 完成请求后才写入真实更新时间。

### `WidgetData.kt`

- 有云端核心数据但暂时无法计算近期消耗时，只显示“暂无可计算数据”；
- 不再允许随机俏皮等待文案冒充动态数据；
- 新五层数据参与 `isEmpty()` 判断，避免真实指标被误判为空；
- 保留最近一次真实成功数据，断网时明确标记缓存；
- 数据来源标签只由 Provider 统一添加，避免重复出现“云端·云端”或“本地·本地”。

### `balance_widget_info.xml`

- 正式自动刷新周期为 Android Widget 支持的 30 分钟。

## 实际数据来源

### Kimi / NewAPI

- 云端：剩余次数、总额、已用次数、调用次数、Billing 额度与用量；
- 本地：基于云端累计调用计算近期调用变化。

### MiMo

- 云端：余额及余额构成、累计请求、累计 Token、累计金额、缓存 Token、Web 搜索、RPM、TPM、并发；
- 本地：基于累计请求、Token、金额计算近期消耗。

### DeepSeek

- 云端：API 余额、充值余额、赠送余额，以及网页账户返回的累计金额、Token、预计可用 Token；
- 本地：基于真实累计值计算近期 Token 和金额；没有确认的请求次数字段时不显示次数。

### 爱黄牛

- 云端：余额、累计充值、累计请求、累计 Token、累计实际消耗、并发和账户状态；
- 本地：基于真实累计请求、Token、金额计算近期消耗。

## 真机验收

1. 四张卡片主指标显示“云端·”；
2. 近期消耗显示“本地·”；
3. 百分比显示“本地·”；
4. 辅助数据显示“云端·”；
5. 断网时显示缓存标记，不伪装成新数据；
6. 单击 Widget 立即刷新；
7. 首次统计不出现虚假数字；
8. 产生真实调用并积累快照后，近期数值随刷新变化；
9. 余额、网页登录和断网兜底不得回归。

## 本地执行边界

只允许拉取、检查 HEAD、编译一次、覆盖安装一次、启动和汇报。

禁止修改代码、自动修复、重复编译、提交、推送、卸载或清除应用数据。
