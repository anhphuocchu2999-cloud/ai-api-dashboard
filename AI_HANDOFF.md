# AI API Dashboard｜AI 交接状态

> 最近更新：2026-07-15

## 当前阶段

`Stage 8B-U：设置页展示可获得的数据能力`

- 开发分支：`feature/stage-8b-simple-connection-flow`
- 桌面真实动态数据基础：`fcd7bce1a816f1eec3ab359dfceffdceb20c8e20`
- 产品规则：`14924cc6835b2aabc7c515945942028d2e65850a`
- 能力说明组件：`7fe498a19aa4b13f52eef1ec818da53598cbe9e2`
- 设置页接入：`44bc29013936569aacc9b44ff4b8da722806b2c6`
- 当前状态：代码已推送，尚未完成本地编译、安装和真机验收

## 本轮目标

每个槽位的设置页在“API 连接”和“平台账户（可选）”之间新增“这个槽位能展示什么”。

页面直接说明：

- 可以获得哪些具体数值；
- `云端 API`、`云端账户`、`本地测算`三种来源；
- 当前是自动更新、连接后可用、登录后可用，还是积累后生成；
- 云端数据每30分钟自动刷新，也可点击桌面小组件立即刷新。

## 平台内容

### Kimi / NewAPI
- 云端 API：剩余次数、总额、已用次数、调用次数、Billing额度和用量；
- 本地测算：近1／6／12／24小时调用变化。

### MiMo
- 云端账户：余额构成、累计金额、累计请求、累计Token、缓存Token、Web搜索、RPM、TPM、并发；
- 本地测算：近1／6／12／24小时请求、Token、金额。

### DeepSeek
- 云端 API：总余额、充值余额、赠送余额；
- 云端账户：本月消耗、累计消耗、本月Token、预计可用Token；
- 本地测算：近1／6／12／24小时Token和金额。

### 爱黄牛
- 云端 API：累计请求、累计Token、累计实际消耗；
- 云端账户：余额、累计充值、并发、账户状态、最近活跃；
- 本地测算：近1／6／12／24小时调用、Token、金额。

## 修改文件

- `PROJECT_STAGE_8B_U.md`
- `SlotDataCapability.kt`
- `MainActivity.kt`

上一轮桌面真实动态数据改动继续保留。

## 真机验收

1. 任意槽位设置页出现“这个槽位能展示什么”；
2. DeepSeek地址显示对应四组数据；
3. 未登录时账户数据写“登录后可用”；
4. 登录成功后改为“已接入 · 自动更新”；
5. 本地近期数据写“积累后自动生成”；
6. MiMo、爱黄牛、Kimi分别显示对应能力；
7. 未识别服务不显示虚构余额能力；
8. 模型检测、账户登录和Widget刷新不回归。

## 本地执行边界

仅拉取、检查HEAD、编译一次、覆盖安装一次、启动和汇报。不要修改代码、重复编译、卸载或清除应用数据。
