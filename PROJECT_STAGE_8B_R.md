# Stage 8B-R｜API 基础连接 + 平台账户增强

> 用户已于 2026-07-15 确认最终交互方案并要求直接开发。

## 目标

配置页只向普通用户提供两个同时存在、互相补充的入口：

1. API 连接；
2. 平台账户登录（可选增强）。

两者不是二选一模式。API 是基础接入，平台账户登录用于补充更丰富的余额、消费、Token 和请求次数等信息。

## 槽位规则

- 固定保留四个槽位；
- 每个槽位使用开关决定是否显示；
- 关闭槽位不删除配置、授权、缓存和历史数据；
- 首页只显示槽位编号、模型名称、连接状态、开关和设置按钮；
- 点击设置后进入单独的槽位编辑页；
- 四个槽位必须使用完全一致的配置结构和状态表达。

## API 连接

编辑页默认始终展示：

- API 地址；
- API Key；
- 检测并选择模型。

检测成功后：

- 单模型自动选择；
- 多模型弹窗选择；
- 服务类型和技术协议由 API 地址在后台自动识别；
- 不要求用户预先选择 Kimi、MiMo、DeepSeek、爱黄牛或其他协议名称；
- 接口实际提供什么数据，就读取什么数据。

## 平台账户登录

API 区域下方始终保留“平台账户（可选）”说明：

> 想获得更完整的余额、消费和用量信息，可以再登录平台账户。API 连接不会受到影响。

系统根据 API 地址自动判断是否存在已验证的官网登录方案。用户不选择服务商，点击“登录平台账户”后直接打开系统自动匹配的官网。

匹配成功时，只显示：

- 当前平台账户是否已连接；
- 登录／重新登录平台账户；
- 断开平台账户；
- 登录后可补充的数据摘要。

没有匹配到已验证网页登录方案时，说明 API 连接仍可正常使用，并禁用登录按钮。

## 授权归属

- 网页授权属于平台账户，不属于历史槽位名称；
- 同一平台登录一次后，所有当前使用该平台 API 地址的槽位都应识别到同一登录状态；
- 旧授权若只存在于历史槽位，需复制迁移到平台公共授权 Key；
- 为兼容当前 Widget 固定槽位读取，平台公共授权会同步复制到所有绑定该平台的槽位；
- 切换 API 地址时，不得把其他平台的 Cookie 或 Token 误判为当前平台授权；
- 断开平台账户时，应同时清除平台公共授权和所有绑定槽位的兼容副本；
- 不删除或打印 API Key、Cookie、Bearer Token 原文。

## Billing 与账户数据

Billing、Usage、余额、Token、请求次数都不是第三种连接方式，而是连接成功后自动读取的数据：

- Kimi / NewAPI：通过 API Key 读取已验证的 Billing 数据；
- MiMo：通过网页登录状态读取余额、本月消费、Token 和请求次数；
- DeepSeek：通过 API 和网页登录状态组合读取余额、本月消费、累计消费和 Token；
- 爱黄牛：通过 API 和网页登录状态读取余额、用量、请求次数和 Token。

## 普通界面禁止出现

- 服务商常驻选择区；
- 服务商选择弹窗；
- 技术协议选择；
- API／网页登录二选一切换；
- 手动 Cookie 输入框；
- 手动 Bearer Token 输入框；
- 保存授权技术按钮；
- 独立 Billing 配置；
- API / WEB_AUTH / BILLING 枚举；
- 原始接口路径和字段表；
- 面向开发者的 Build 编号。

## 兼容要求

- 不清除现有 API Key、Cookie、Bearer Token、模型和缓存；
- 不修改 Widget 四卡布局；
- 不修改 AdapterFactory 路由原则；
- 不新增数据库、WorkManager 或第二套授权体系；
- 不合并到 main；
- 业务代码完成后只编译一次、覆盖安装一次；
- 禁止卸载和清除应用数据。
