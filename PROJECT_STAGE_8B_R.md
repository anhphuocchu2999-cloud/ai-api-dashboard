# Stage 8B-R｜简化连接流程（PROJECT.md 补充规范）

> 用户已于 2026-07-14 明确确认本方案并要求直接开发。

## 目标

配置页不再向普通用户平铺 API、Cookie、Bearer Token、Billing、数据能力模型等内部概念。

每个模型槽位只提供两种用户可理解的连接入口：

1. 使用 API
2. 登录官网账户

Billing、Usage、余额、Token、请求次数等均作为连接成功后自动读取的数据，不再作为第三种连接模式。

## 界面层级

配置首页只显示：

- 四个槽位；
- 每个槽位的模型或服务名称；
- 是否已连接；
- 显示开关；
- 设置／编辑按钮。

首页不得直接展开服务选择、API 输入框、模型列表或官网登录控件。

点击某一槽位的“设置／编辑”后，进入该槽位的独立编辑页面。编辑页一次只处理一个槽位，不在同一长页面堆叠四套配置。

## 槽位规则

- 继续保留四个固定槽位；
- 每个槽位使用开关决定是否启用；
- 不增加“加减模型数量”的独立计数页面；
- 关闭的槽位不参与 Widget 刷新，但配置和历史数据不删除；
- 每个槽位仍代表独立 Model Instance。

## API 模式

用户只看到：

- 服务类型；
- API Base URL；
- API Key；
- 检测连接；
- 模型选择结果。

检测成功后：

- 单模型自动选择；
- 多模型弹窗供用户选择；
- 有什么真实数据就展示什么；
- 不要求用户理解 Billing 接口或 Adapter。

## 官网账户模式

对已完成真实验证的服务开放：

- MiMo；
- DeepSeek 官方；
- 爱黄牛。

用户只看到：

- 连接官网账户／重新连接；
- 当前是否已连接；
- 登录后能够读取的账户数据摘要；
- 断开账户。

Cookie、Bearer Token、Session、手动保存授权默认不在普通界面展示。

Kimi / NewAPI 当前没有已验证的网页登录方案，因此只显示 API 模式，不显示无效入口。

## Billing 展示原则

- Billing 不是连接方式；
- 不在配置页显示单独 Billing 卡片；
- Kimi / NewAPI 通过 API Key 自动读取已验证的 Billing 接口；
- MiMo 和 DeepSeek 通过网页登录状态读取已验证的官网账户用量接口；
- 爱黄牛读取其已验证的账户与用量数据；
- 未验证数据不伪造，不显示 0 冒充。

## 普通界面禁止出现

- 手动 Cookie 输入框；
- 手动 Bearer Token 输入框；
- “保存授权”技术按钮；
- Subscription / Usage Billing 接口说明；
- API / WEB_AUTH / BILLING 枚举；
- 原始接口路径；
- 技术字段表；
- 面向开发者的 Build 编号。

## 兼容要求

- 不清除现有 API Key、Cookie、Bearer Token、模型和缓存；
- 不修改 Widget 四卡布局；
- 不修改 AdapterFactory 路由原则；
- 不合并到 main；
- 业务代码完成后只编译一次、覆盖安装一次；
- 禁止卸载和清除应用数据。
