# Stage 8B-U｜设置页数据能力说明

## 用户目标

四个槽位的设置页必须直接告诉用户：

1. 当前服务可以在桌面小组件展示哪些真实数值；
2. 哪些数值由目标平台接口直接返回；
3. 哪些数值由手机基于平台真实累计值计算；
4. 哪些数据连接 API 后即可获得；
5. 哪些数据需要登录平台账户后才能获得；
6. 哪些数据会自动更新；
7. 哪些本地近期数据需要先积累一段真实历史。

## 页面位置

在“API 连接”和“平台账户（可选）”之间增加独立卡片：

`这个槽位能展示什么`

该卡片根据 API 地址自动识别服务，不要求用户选择平台。

## 来源标签

- `云端 API`：使用 API Key 请求目标平台接口直接返回；
- `云端账户`：登录目标平台官网后由账户接口直接返回；
- `本地测算`：手机保存多个时间点的真实累计值并做差值计算。

## 更新状态

- `已接入 · 自动更新`：当前凭据已经满足，可随 Widget 刷新更新；
- `连接 API 后可用`：需要完成 API 地址、API Key 和模型连接；
- `登录后可用`：需要登录平台账户；
- `积累后自动生成`：需要先保存至少两次真实累计快照；
- `当前服务未识别`：不承诺平台没有真实提供的指标。

## 刷新说明

- 云端数据由桌面 Widget 每 30 分钟自动刷新；
- 用户点击桌面 Widget 可立即刷新；
- 本地测算只使用平台真实累计数字；
- 历史不足时不显示虚假近 1／6／12／24 小时数据。

## 平台能力

### Kimi / NewAPI

- 云端 API：剩余次数、总额、已用次数、调用次数；
- 云端 API：Billing 额度与 Billing 用量；
- 本地测算：近 1／6／12／24 小时调用变化。

### MiMo

- 云端账户：余额、赠送余额、现金余额、冻结余额、可用透支；
- 云端账户：累计金额、累计请求、累计 Token；
- 云端账户：缓存 Token、Web 搜索、RPM、TPM、并发；
- 本地测算：近 1／6／12／24 小时请求、Token、金额。

### DeepSeek

- 云端 API：总余额、充值余额、赠送余额；
- 云端账户：本月消耗、累计消耗、本月 Token、预计可用 Token；
- 本地测算：近 1／6／12／24 小时 Token 与金额；
- 未确认可靠累计调用字段时，不展示调用次数。

### 爱黄牛

- 云端账户：余额、累计充值、并发、账户状态、最近活跃；
- 云端 API：累计请求、累计 Token、累计实际消耗；
- 本地测算：近 1／6／12／24 小时调用、Token、金额。

## 真实性约束

- 页面展示的是当前代码已经真实接入或明确支持的数据能力；
- 不使用演示数字；
- 不把本地计算冒充平台直接返回；
- 不把登录后能力显示成当前已经可用；
- 当前服务无法识别时，只展示通用 API 能力，不猜测余额和账单能力。
