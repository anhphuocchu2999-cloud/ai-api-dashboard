# AI API Dashboard — AGENTS.md

本文件定义 AI 开发代理的工作规则。

开发前必须先阅读：

1. AGENTS.md
2. PROJECT.md
3. 当前任务直接相关的源码

不得跳过。

---

# 一、最高原则

所有开发行为按以下优先级执行：

真实性
↓
用户确认
↓
阶段纪律
↓
功能正确
↓
架构一致
↓
体验优化
↓
UI 美化

任何情况下：

不得假装成功。
不得猜测代码。
不得跳过验证。

---

# 二、任务边界

1. 每次只完成一个明确能力。

例如：

- Widget
- Refresh
- Configuration
- Test Connection
- API Integration
- MiMo WebView Login
- Cookie Save
- Provider Detection
- Widget Resize

禁止一次同时开发多个独立能力。

2. 用户未明确要求的功能，不得顺手开发。

3. 不得因为当前任务顺便重构整个项目。

4. 不得自动扩大任务范围。

5. 当前任务完成后必须停止并等待用户验收。

---

# 三、需求变更流程

任何新的正式产品需求必须遵循：

讨论需求
↓
形成明确方案
↓
更新 PROJECT.md
↓
用户确认
↓
开始开发

禁止：

- 边讨论边修改业务代码
- 用户尚未确认就提前开发
- 根据聊天内容自行猜测需求并开发

如果用户只要求修改：

- PROJECT.md
- AGENTS.md
- README

通常无需编译。

---

# 四、源码读取规范

1. 只读取当前任务需要的文件。

2. 禁止无目的扫描整个项目。

3. 禁止递归遍历全部目录。

4. 禁止为了一个小 Bug 阅读全部源码。

5. 修改前必须先读取相关源码。

6. 未读取相关源码，禁止汇报：

"已定位问题"。

7. 不得根据文件名猜测实现。

---

# 五、开发顺序

始终遵循：

可编译
↓
功能正确
↓
体验优化
↓
UI 美化

禁止：

- 功能尚未正确就进行大规模视觉美化
- 为了 UI 提前引入复杂架构
- 同时修改功能逻辑和多个视觉系统

---

# 六、架构约束

## 1. Widget 卡片

Widget 的每一张卡片代表：

Model Instance

不代表：

- 平台
- 公司
- 中转站

不得根据 Widget 显示标题判断 Adapter。

---

## 2. Adapter 路由

所有 Adapter 必须统一通过：

AdapterFactory

进行路由。

禁止在：

BalanceWidgetProvider

或其他 Widget 渲染层中写：

if MiMo
if DeepSeek
if Kimi
if OpenAI

---

## 3. Widget Provider 职责

Widget Provider 只负责：

- 读取配置
- 获取模型实例
- 调用数据层
- 读取缓存
- 渲染 Widget
- 处理 Widget 生命周期

禁止在 Widget 层：

- 解析平台 JSON
- 编写余额接口协议
- 判断平台协议
- 直接处理 Cookie 协议
- 硬编码平台接口

---

## 4. Adapter 职责

Adapter 可以负责：

- HTTP 请求
- JSON 解析
- 平台协议
- 错误转换
- 数据标准化
- WidgetData 转换

特殊认证逻辑应尽可能与普通 Widget 渲染逻辑分离。

---

## 5. 新平台接入

新增服务时，必须按以下顺序判断：

① 是否兼容已有协议？

是：
复用已有 Adapter。

② 是否属于已有平台家族？

是：
复用对应 Family Adapter。

③ 是否只有特殊认证或特殊数据接口？

是：
增加最小必要扩展。

④ 是否完全不同？

才允许新增独立 Provider / Adapter。

禁止：

每新增一个域名就新增一个 Adapter。

---

## 6. 同协议复用

同一种协议允许多个模型共用同一个 Adapter。

例如同一个 OpenAI-Compatible Base 可以提供：

- kimi-k2.6
- deepseek-v4-pro
- qwen-max
- glm-4.5

不得因为 Model 名称不同就分别创建 Adapter。

---

## 7. 修改已有 Adapter

新增平台或模型时：

原则上不得修改其他 Adapter 的业务逻辑。

确需修改公共能力时：

必须明确说明原因。

不得为了接入一个新平台破坏已有平台。

---

# 七、依赖与项目结构

1. 不随意增加依赖。

2. 新依赖必须满足：

- 当前任务确实需要
- Android 原生能力明显不足
- 不造成不必要复杂化

3. 不随意修改项目结构。

4. 禁止因为一个小功能：

- 大规模重构
- 更换整体架构
- 引入复杂 DI
- 引入大型框架
- 引入数据库
- 引入 WorkManager

除非当前需求已明确讨论并得到用户确认。

---

# 八、网络请求规范

1. 相同 Host 默认串行请求。

2. 同一 Host 两次请求默认间隔不少于：

500ms

如目标平台有特殊要求，可单独配置。

3. 禁止多个线程无控制地同时请求同一个 Host。

4. 请求失败禁止无限重试。

5. 禁止死循环重试。

---

## HTTP 429

用户提示：

请求过于频繁

必须：

保留最近一次成功数据。

---

## HTTP 5xx

用户提示：

服务器异常

---

## 网络异常

用户提示：

网络异常

---

## 未知异常

用户提示：

请求失败

---

技术异常默认不得直接展示给普通用户。

---

# 九、缓存规范

1. 每个模型实例独立缓存。

2. 不同 Provider 不共享错误状态。

3. 默认缓存有效期：

30 秒

4. Widget 刷新时：

优先显示已有缓存
↓
再后台获取最新数据

5. 请求失败不得清空已有成功数据。

6. 必须保留：

Last Successful Data

7. Cookie / Session 失效不得删除历史成功缓存。

8. 网络异常不得导致 Widget 突然变成空白。

---

# 十、敏感信息规范

可能涉及：

- API Key
- Cookie
- Session
- OAuth Token

禁止：

- 输出到普通日志
- 在错误提示中完整显示
- 在 UI 中默认完整展示
- 上传到开发者服务器
- 因调试随意复制到无关文件

不得在未经用户确认的情况下新增云端上传能力。

---

# 十一、Bug 修复规范

1. 每次只修复一个 Bug。

2. Bug 未定位前禁止修改代码。

3. 修改前先分析。

4. 分析必须基于真实源码或真实运行结果。

5. 未读取相关源码，不得声称：

已定位问题。

6. 一个 Bug 未验收，不得开始下一个 Bug。

7. 禁止一次提交多个未验证修复。

8. 用户反馈与开发汇报不一致时：

第一优先检查：

是否运行的是最新安装版本。

不得立即假设业务代码有问题。

---

# 十二、代码修改后的验证流程

任何业务代码修改后必须执行：

修改
↓
Build Debug APK
↓
自动覆盖安装
↓
汇报
↓
等待用户验收

不得自动进入下一阶段。

---

# 十三、编译规范

每次任务：

assembleDebug 仅执行一次。

禁止：

- 无意义重复编译
- 自动连续编译多次
- while 重试
- sleep 死循环

编译失败时：

优先分析最后的有效错误信息。

不得因为编译失败同时修改多个无关模块。

---

# 十四、安装规范

APK 安装仅执行一次。

安装失败：

立即停止。

优先检查：

- APK 是否真实存在
- APK 路径是否正确
- 安装权限
- 设备连接状态

禁止：

- 无限 adb install
- 无限 pm install
- 无限 cp
- 自动重复安装
- 因安装失败修改业务代码

安装失败只允许：

分析安装原因
+
明确汇报

---

# 十五、版本验证规范

每次代码编译后的 App 首页必须显示：

- Current Stage
- Build Version

例如：

Stage 5B-2

Build 20260709-001

用户反馈的界面与开发汇报不一致时：

首先检查是否运行最新版本。

不得直接修改代码。

---

# 十六、用户提示规范

所有面向普通用户的提示默认使用中文。

尽量回答：

① 发生了什么

② 为什么

③ 怎么解决

例如：

🌐 无法连接服务器

可能原因：
API Base 地址当前无法访问。

建议：
请检查 API Base 地址后重试。

默认禁止直接展示：

- Java Exception
- StackTrace
- 原始英文异常
- 大段服务器报错

技术详情仅在用户主动查看时显示。

---

# 十七、真实性原则

禁止以下行为：

1. 未读取源码，汇报"已定位问题"。

2. 未修改代码，汇报"已修复"。

3. 未编译，汇报"编译成功"。

4. 未安装，汇报"安装成功"。

5. 未验证，汇报"功能正常"。

6. 用户未验收，就进入下一阶段。

7. 工具不可用时继续假装开发。

8. 根据猜测修改代码。

9. 将"理论可行"汇报成"已经实现"。

无法确认时必须明确写：

未验证

---

# 十八、阶段管理

每个阶段只完成一个明确目标。

标准流程：

开发
↓
编译
↓
安装
↓
汇报
↓
用户验收

未验收：

不得进入下一阶段。

用户明确验收通过后：

方可开始下一阶段。

---

# 十九、开发汇报格式

每次代码任务结束后统一使用：

当前阶段：

当前版本：

Build 时间：

完成内容：

验证结果：

遗留问题：

下一步建议：

不得自动开始"下一步建议"。

必须等待用户确认。

---

# 二十、执行原则

明确任务后：

- 优先直接执行
- 不进行无意义重复分析
- 不反复询问已经明确的信息
- 不主动扩大任务
- 不随意改变既定产品规则
- 不为了未来需求提前完成巨大重构

始终坚持：

先把一个能力真正做对，
再进入下一个能力。